"use client";

import { ArrowDown, ArrowRight, BellRing, Check, CircleAlert, Clock3, FileCheck2, FileLock2, Fingerprint, GitBranch, History, LockKeyhole, MessageSquareText, Radio, Search, ShieldCheck, TimerReset, UserCheck, X } from "lucide-react";
import { useMemo, useState } from "react";
import { Button } from "@/components/ui/button";
import { IncidentTimeline } from "./incident-timeline";

export function Hero() { return <section id="top" className="mx-auto grid max-w-7xl items-center gap-12 px-6 py-16 lg:min-h-[720px] lg:grid-cols-12"><div className="reveal lg:col-span-5"><div className="inline-flex items-center gap-2 rounded-full border border-border bg-card px-3 py-2 font-mono text-[10px] uppercase tracking-[0.15em] text-primary shadow-clay-sm"><span className="pulse-dot size-2 rounded-full bg-signal" /> Event-driven maintenance records</div><h1 className="mt-6 max-w-[10ch] font-display text-5xl font-bold leading-[1.03] sm:text-6xl">Keep the trace. Prove what happened.</h1><p className="mt-6 max-w-lg text-lg leading-relaxed text-muted-foreground">Tracehold turns unresolved maintenance complaints into a persistent, auditable trail of actions, delays, recurring issues, and evidence.</p><div className="mt-8 flex flex-wrap gap-3"><Button onClick={() => document.querySelector("#story")?.scrollIntoView({ behavior: "smooth" })}>Follow the complaint <ArrowDown size={16} /></Button><Button variant="outline" onClick={() => document.querySelector("#architecture")?.scrollIntoView({ behavior: "smooth" })}>View architecture <ArrowRight size={16} /></Button></div></div><div className="reveal-delay lg:col-span-7"><IncidentTimeline /></div></section>; }

export function Workflow() { const stages = ["MESSAGE", "TICKET", "EVENT", "DELAY", "ESCALATION", "PATTERN", "EVIDENCE"]; return <section id="workflow" className="border-y border-border bg-surface py-24"><div className="mx-auto max-w-7xl px-6"><p className="eyebrow">What gets remembered</p><h2 className="mt-4 max-w-4xl font-display text-4xl font-bold leading-tight sm:text-5xl">Most systems remember the ticket.<br /><span className="text-primary">Tracehold remembers what happened around it.</span></h2><div className="mt-16 grid gap-3 sm:grid-cols-7">{stages.map((stage, index) => <div key={stage} className="group flex items-center gap-3 sm:block"><div className={`grid min-h-24 flex-1 place-items-center rounded-2xl border px-3 text-center font-mono text-[11px] font-medium shadow-clay-sm transition-transform hover:-translate-y-1 ${index === 4 ? "border-signal/30 bg-signal text-signal-foreground shadow-signal" : index === 6 ? "border-primary/20 bg-primary text-primary-foreground" : "border-border bg-card"}`}>{stage}</div>{index < stages.length - 1 && <ArrowRight className="shrink-0 text-muted-foreground sm:mx-auto sm:mt-3" size={16} />}</div>)}</div></div></section>; }

export function Story() { const items = [["DAY 0", "ComplaintCreated", "A tenant reports water spreading across the bathroom ceiling. Photos and the original message are timestamped immediately."], ["DAY 1", "No response", "Silence becomes observable and the unresolved complaint escalates."], ["DAY 3", "Evidence workflow", "Historical search connects earlier leaks in Unit 304."]] as const; const [active, setActive] = useState(0); return <section id="story" className="mx-auto max-w-7xl px-6 py-24"><div className="grid gap-12 lg:grid-cols-12 lg:items-start"><div className="lg:col-span-4"><p className="eyebrow text-signal">The ignored complaint</p><h2 className="mt-4 font-display text-4xl font-bold">What happens when nobody responds?</h2><p className="mt-5 leading-relaxed text-muted-foreground">Silence is an event. Tracehold watches the time between actions and grows the record when nothing happens.</p></div><div className="lg:col-span-8"><div className="rounded-[28px] bg-ink p-6 text-mist shadow-ink sm:p-9"><div className="flex gap-2">{items.map(([day], index) => <button type="button" key={day} onClick={() => setActive(index)} className={`flex-1 rounded-xl px-3 py-3 font-mono text-[11px] ${active === index ? "bg-signal text-signal-foreground" : "bg-ink-elevated text-mist/60"}`}>{day}</button>)}</div><div className="mt-8 grid gap-8 sm:grid-cols-[1fr_auto]"><div><p className="font-mono text-[10px] uppercase text-forensic-light">{items[active][0]}</p><h3 className="mt-3 font-display text-3xl font-bold">{items[active][1]}</h3><p className="mt-4 max-w-lg leading-relaxed text-mist/65">{items[active][2]}</p></div><div className="grid size-28 place-items-center rounded-2xl bg-ink-elevated"><span className="font-display text-4xl text-signal">0{active + 1} / 3</span></div></div></div></div></div></section>; }

export function SignalMap() { const branches = [[Clock3, "Response clock", "24h window started", "primary"], [BellRing, "Escalation", "Recipients prepared", "signal"], [History, "Pattern search", "4 records linked", "primary"], [FileLock2, "Evidence", "Draft assembled", "primary"]] as const; return <section id="signal-map" className="mx-auto max-w-7xl px-6 py-24"><div className="grid gap-12 lg:grid-cols-12 lg:items-center"><div className="lg:col-span-4"><p className="eyebrow">Signal map</p><h2 className="mt-4 font-display text-4xl font-bold">One report starts four independent watches.</h2><p className="mt-5 leading-relaxed text-muted-foreground">The complaint is recorded once, then distributed into clocks, escalation rules, historical search, and evidence preparation.</p><div className="mt-8 flex items-center gap-3 font-mono text-[10px] uppercase text-muted-foreground"><Radio size={15} className="text-signal" /> Live event · ComplaintCreated</div></div><div className="lg:col-span-8"><div className="rounded-[28px] border border-border bg-card p-6 shadow-clay sm:p-10"><div className="mx-auto flex max-w-xs items-center gap-4 rounded-2xl bg-ink p-4 text-mist shadow-ink"><span className="grid size-12 place-items-center rounded-xl bg-signal text-signal-foreground"><MessageSquareText size={22} /></span><div><p className="font-mono text-[10px] uppercase text-forensic-light">Source event</p><p className="mt-1 font-semibold">Complaint TR-1042</p></div></div><div className="mx-auto h-10 w-px bg-timeline" /><div className="grid gap-4 sm:grid-cols-2">{branches.map(([Icon, title, detail, tone], index) => <div key={title} className={`relative flex min-h-28 items-center gap-4 rounded-2xl border p-4 shadow-clay-sm ${tone === "signal" ? "border-signal/30 bg-signal-soft" : "border-primary/20 bg-primary-soft"}`}><span className={`grid size-11 shrink-0 place-items-center rounded-xl ${tone === "signal" ? "bg-signal text-signal-foreground" : "bg-primary text-primary-foreground"}`}><Icon size={19} /></span><div><p className="text-sm font-semibold">{title}</p><p className="mt-1 font-mono text-[10px] uppercase text-muted-foreground">{detail}</p></div><span className="absolute right-4 top-4 font-mono text-[9px] text-muted-foreground">0{index + 1}</span></div>)}</div></div></div></div></section>; }

export function ResponseWindow() { const [hour, setHour] = useState<0 | 12 | 24>(24); const states = { 0: ["Recorded", "The clock starts", "The complaint and expected response window are fixed in the record.", "Tenant · Property manager", "6%"], 12: ["Watching", "The window is open", "No response has arrived. Tracehold continues watching.", "Tenant · Property manager", "50%"], 24: ["Escalated", "Silence becomes an event", "The acknowledgement deadline expires and the delay is recorded.", "Tenant · Manager · Maintenance lead", "100%"] } as const; const current = states[hour]; return <section id="response-window" className="border-y border-border bg-surface py-24"><div className="mx-auto max-w-7xl px-6"><p className="eyebrow text-signal">Response window</p><h2 className="mt-4 max-w-3xl font-display text-4xl font-bold">A deadline is not a note. It is a moving part of the record.</h2><div className="mt-12 grid gap-6 lg:grid-cols-[1.4fr_.6fr]"><div className="rounded-[28px] border border-border bg-card p-6 shadow-clay sm:p-9"><div className="grid grid-cols-3 gap-2">{([0, 12, 24] as const).map((value) => <Button key={value} variant={hour === value ? "default" : "outline"} onClick={() => setHour(value)} className="w-full">{value === 0 ? "Reported" : `+${value} hours`}</Button>)}</div><div className="mt-12 h-2 rounded-full bg-secondary"><span className={`block h-full rounded-full ${hour === 24 ? "bg-signal" : "bg-primary"}`} style={{ width: current[4] }} /></div><div className="mt-10"><p className="font-mono text-[10px] uppercase text-signal">{current[0]}</p><h3 className="mt-2 font-display text-2xl font-bold">{current[1]}</h3><p className="mt-3 text-sm leading-relaxed text-muted-foreground">{current[2]}</p></div></div><div className="rounded-[28px] bg-ink p-6 text-mist shadow-ink sm:p-8"><TimerReset className="text-signal" size={26} /><p className="mt-8 font-mono text-[10px] uppercase text-forensic-light">Visibility at {hour} hours</p><p className="mt-3 font-display text-2xl font-bold">{current[3]}</p><div className="mt-8 space-y-3">{["Original report preserved", "Deadline state recorded", hour === 24 ? "Escalation delivered" : "Escalation standing by"].map((item, index) => <div key={item} className="flex items-center gap-3 border-t border-mist/10 pt-3 text-sm text-mist/65"><Check size={15} className={index === 2 && hour !== 24 ? "text-mist/25" : "text-forensic-light"} />{item}</div>)}</div></div></div></div></section>; }

export function ChainOfCustody() { const records = [[Fingerprint, "BLOCK 01 · 6A2F", "Captured", "Message + attachments", "Immutable"], [GitBranch, "BLOCK 02 · 91DC", "Identified", "Actor + timestamp", "Linked"], [ShieldCheck, "BLOCK 03 · C044", "Sealed", "Original event retained", "Verified"], [LockKeyhole, "BLOCK 04 · EF17", "Verified", "Policy decision attached", "Valid"]] as const; return <section id="chain" className="mx-auto max-w-7xl px-6 py-24"><div className="grid gap-12 lg:grid-cols-12"><div className="lg:col-span-4"><p className="eyebrow">Chain of custody</p><h2 className="mt-4 font-display text-4xl font-bold">Evidence stays useful because every handoff is visible.</h2><p className="mt-5 leading-relaxed text-muted-foreground">Each step adds context without rewriting what came before. The sequence remains inspectable from first report to policy decision.</p></div><div className="lg:col-span-8"><div className="overflow-hidden rounded-[28px] border border-border bg-card shadow-clay">{records.map(([Icon, block, title, detail, status]) => <div key={block} className="grid grid-cols-[2.5rem_1fr_auto] items-center gap-4 border-b border-border px-5 py-5 last:border-0"><span className="grid size-9 place-items-center rounded-xl bg-primary-soft text-primary"><Icon size={16} /></span><span><small className="font-mono text-[10px] text-primary">{block}</small><strong className="mt-1 block text-sm">{title}</strong><small className="text-xs text-muted-foreground">{detail}</small></span><span className="rounded-full bg-secondary px-2 py-1 font-mono text-[9px] uppercase text-primary">{status}</span></div>)}</div><p className="mt-4 font-mono text-[10px] uppercase text-muted-foreground">TR-1042 · Event chain verified · 4 / 4 signatures valid</p></div></div></section>; }

export function Authorization() { const rows = [[UserCheck, "Tenant", "Read own complaints", true], [ShieldCheck, "Contractor", "Update work", true], [LockKeyhole, "Landlord", "Review escalation", true], [UserCheck, "Admin", "Review policy", true], [X, "Contractor", "Close ticket", false]] as const; return <section id="authorization" className="border-y border-border bg-surface py-24"><div className="mx-auto max-w-7xl px-6"><div className="flex max-w-3xl items-end justify-between gap-6"><div><p className="eyebrow">Cedar authorization</p><h2 className="mt-4 font-display text-4xl font-bold">Not everyone gets to close the record.</h2><p className="mt-5 leading-relaxed text-muted-foreground">Protected actions are evaluated against the actor, the action, and the exact record—not a hidden checkbox in the interface.</p></div><LockKeyhole className="hidden text-primary sm:block" size={42} /></div><div className="mt-12 overflow-hidden rounded-[28px] border border-border bg-card shadow-clay"><div className="grid grid-cols-[2rem_1fr_1fr_auto] gap-4 border-b border-border bg-surface px-5 py-4 font-mono text-[10px] uppercase text-muted-foreground"><span /><span>Actor</span><span>Action · Ticket #TR-1042</span><span>Cedar decision</span></div>{rows.map(([Icon, principal, action, allowed]) => <div key={`${principal}-${action}`} className="grid grid-cols-[2rem_1fr_1fr_auto] items-center gap-4 border-b border-border px-5 py-4 last:border-0"><Icon size={17} className="text-primary" /><span className="text-sm font-semibold">{principal}</span><span className="text-sm text-muted-foreground">{action}</span><span className={`rounded-full px-3 py-1 font-mono text-[10px] uppercase ${allowed ? "bg-primary-soft text-primary" : "bg-signal-soft text-signal"}`}>{allowed ? "Permit" : "Deny"}</span></div>)}</div><p className="mt-4 text-sm text-muted-foreground">The assigned contractor can update work, but cannot close the record.</p></div></section>; }

export function OutcomeComparison() { return <section id="outcomes" className="mx-auto max-w-7xl px-6 py-24"><p className="eyebrow">Two possible outcomes</p><h2 className="mt-4 max-w-3xl font-display text-4xl font-bold">The same complaint. A completely different record.</h2><div className="mt-12 grid gap-6 md:grid-cols-2"><div className="rounded-[28px] border border-border bg-card p-7 shadow-clay"><div className="flex items-center gap-3 text-muted-foreground"><X size={20} /><p className="eyebrow">Ordinary maintenance thread</p></div><p className="mt-8 font-display text-2xl font-bold">The trail thins out</p><ul className="mt-6 space-y-3 text-sm text-muted-foreground"><li>Message disappears into an inbox</li><li>No shared response deadline</li><li>Earlier complaints stay disconnected</li><li>Resolution depends on memory</li></ul></div><div className="rounded-[28px] bg-primary p-7 text-primary-foreground shadow-clay"><div className="flex items-center gap-3"><Check size={20} /><p className="eyebrow">Tracehold record</p></div><p className="mt-8 font-display text-2xl font-bold">The trail grows stronger</p><ul className="mt-6 space-y-3 text-sm opacity-80"><li>Original report remains attached</li><li>Silence creates an escalation event</li><li>Related complaints form a pattern</li><li>Evidence follows a verifiable chain</li></ul></div></div></section>; }

export function Principles() { const principles = [[Fingerprint, "Persistent", "Every important action becomes part of the record."], [Clock3, "Event-driven", "Important changes create events instead of disappearing into state."], [History, "Historical", "Every complaint can be understood beside the complaints before it."], [UserCheck, "Controlled", "Authorization determines who can perform protected actions."], [ShieldCheck, "Explainable", "AI drafts summaries without becoming the source of truth."]] as const; return <section id="principles" className="border-y border-border bg-surface py-24"><div className="mx-auto max-w-7xl px-6"><p className="eyebrow">Built for the moment after</p><h2 className="mt-4 max-w-3xl font-display text-4xl font-bold">The important part isn&apos;t when the complaint is created. It&apos;s what happens after.</h2><div className="mt-12 grid gap-4 sm:grid-cols-2 lg:grid-cols-5">{principles.map(([Icon, title, detail], index) => <div key={title} className="rounded-2xl border border-border bg-card p-5 shadow-clay-sm"><Icon className="text-primary" size={22} /><span className="mt-5 block font-mono text-[10px] text-muted-foreground">0{index + 1}</span><h3 className="mt-2 font-display text-xl font-bold">{title}</h3><p className="mt-3 text-sm leading-relaxed text-muted-foreground">{detail}</p></div>)}</div></div></section>; }

export function EvidenceDetails() { return <section className="mx-auto max-w-7xl px-6 pb-24"><div className="grid gap-6 sm:grid-cols-3"><div className="rounded-2xl border border-border bg-card p-5 shadow-clay-sm"><p className="document-label">Recurring pattern</p><p className="mt-3 text-sm leading-relaxed text-muted-foreground">Four water-related incidents were reported in the same unit over eight months, including three involving the bathroom ceiling.</p></div><div className="rounded-2xl border border-border bg-card p-5 shadow-clay-sm"><p className="document-label">Notice draft</p><p className="mt-3 text-sm leading-relaxed text-muted-foreground">This notice records an unresolved recurring water intrusion issue at Unit 304 and the response history attached to complaint TR-1042.</p></div><div className="rounded-2xl border border-border bg-card p-5 shadow-clay-sm"><p className="document-label">Pattern summary</p><p className="mt-3 font-mono text-sm text-primary">4 incidents · 8 months · 1 unit</p></div></div></section>; }

export function HistorySearch() { const [query, setQuery] = useState("water leak bathroom"); const records = [["JAN 14", "Bathroom ceiling moisture", "TR-0871"], ["MAR 03", "Pipe leakage near bathroom", "TR-0918"], ["MAY 27", "Water damage around ceiling", "TR-0994"], ["AUG 19", "Bathroom ceiling leak", "TR-1042"]] as const; const visible = useMemo(() => query.trim().length > 2, [query]); return <section id="history" className="border-y border-border bg-surface py-24"><div className="mx-auto grid max-w-7xl gap-12 px-6 lg:grid-cols-12"><div className="lg:col-span-5"><p className="eyebrow">Historical context</p><h2 className="mt-4 font-display text-4xl font-bold">One complaint is an issue. Four complaints are a pattern.</h2><p className="mt-5 leading-relaxed text-muted-foreground">Search joins the current incident to the maintenance history that changes its meaning.</p><div className="mt-8 inline-flex items-center gap-3 rounded-2xl bg-signal-soft px-4 py-3 text-signal"><CircleAlert size={18} /><span className="font-mono text-xs">PATTERN DETECTED · 4 INCIDENTS</span></div></div><div className="lg:col-span-7"><div className="rounded-[28px] border border-border bg-card p-6 shadow-clay sm:p-8"><label htmlFor="history-query" className="eyebrow">Search maintenance history</label><div className="mt-4 flex items-center gap-3 rounded-xl bg-secondary px-4 py-3"><Search size={18} className="text-primary" /><input id="history-query" value={query} onChange={(event) => setQuery(event.target.value)} className="min-w-0 flex-1 bg-transparent text-sm outline-none" placeholder="Search complaint records..." /></div><div className="mt-5 divide-y divide-border">{visible ? records.map(([date, title, id]) => <div key={id} className="grid grid-cols-[4rem_1fr_auto] items-center gap-3 py-4"><span className="font-mono text-[10px] text-muted-foreground">{date}</span><span className="text-sm font-medium">{title}</span><span className="font-mono text-[10px] text-primary">{id}</span></div>) : <p className="py-10 text-center text-sm text-muted-foreground">Enter at least three characters to search.</p>}</div></div></div></div></section>; }

export function EvidenceSection() { return <section id="evidence" className="mx-auto max-w-7xl px-6 py-24"><div className="mb-12 max-w-3xl"><p className="eyebrow">Evidence workspace</p><h2 className="mt-4 font-display text-4xl font-bold">The record becomes something you can use.</h2></div><div className="grid gap-6 lg:grid-cols-[0.72fr_1.28fr]"><div className="space-y-3">{["Source events", "Incident timeline", "Related history", "Evidence summary"].map((label, index) => <div key={label} className={`flex items-center gap-4 rounded-2xl border p-4 shadow-clay-sm ${index === 3 ? "border-primary/20 bg-primary text-primary-foreground" : "border-border bg-card"}`}><span className="grid size-9 place-items-center rounded-xl bg-secondary font-mono text-[10px] text-primary">0{index + 1}</span><span className="text-sm font-semibold">{label}</span>{index < 3 && <ArrowDown size={14} className="ml-auto" />}</div>)}</div><article className="rounded-[28px] border border-border bg-card p-6 shadow-clay sm:p-9"><div className="flex flex-wrap items-start justify-between gap-4 border-b border-border pb-5"><div><p className="eyebrow">Evidence summary</p><h3 className="mt-2 font-display text-2xl font-bold">Bathroom ceiling leak</h3></div><span className="rounded-full bg-primary-soft px-3 py-1 font-mono text-[10px] uppercase text-primary">AI-generated draft</span></div><dl className="mt-6 grid gap-5 sm:grid-cols-2"><div><dt className="document-label">First reported</dt><dd className="mt-1 text-sm font-semibold">January 14, 2026</dd></div><div><dt className="document-label">Related incidents</dt><dd className="mt-1 text-sm font-semibold">4 in Unit 304</dd></div><div className="sm:col-span-2"><dt className="document-label">Response history</dt><dd className="mt-1 text-sm leading-relaxed text-muted-foreground">No response was recorded within the required 24-hour window. The issue escalated automatically.</dd></div></dl><div className="mt-7 rounded-2xl bg-secondary p-5"><p className="document-label">Notice draft</p><p className="mt-3 text-sm leading-relaxed">This draft records an unresolved recurring water intrusion issue and the response history attached to complaint TR-1042.</p></div></article></div></section>; }

export function Architecture() { const nodes = [["Next.js", "Interface"], ["NestJS", "Application API"], ["Cedar", "Authorization"], ["PostgreSQL", "Source of truth"], ["OpenSearch", "Related history"], ["SQS", "Event queue"], ["Lambda", "Escalation"], ["Strands", "Evidence drafts"]]; return <section id="architecture" className="border-b border-border bg-surface py-24"><div className="mx-auto max-w-7xl px-6"><p className="eyebrow">System architecture</p><h2 className="mt-4 max-w-2xl font-display text-4xl font-bold">The event moves. The record holds.</h2><div className="relative mt-14 overflow-hidden rounded-[28px] border border-border bg-card p-6 shadow-clay sm:p-10"><div className="relative grid gap-4 sm:grid-cols-2 md:grid-cols-4">{nodes.map(([name, role], index) => <div key={name} className={`min-h-28 rounded-2xl border p-4 shadow-clay-sm ${index === 0 || index === 7 ? "border-primary/20 bg-primary text-primary-foreground" : "border-border bg-surface"}`}><p className="font-mono text-[10px] opacity-55">0{index + 1}</p><p className="mt-3 font-display text-lg font-bold">{name}</p><p className="mt-1 text-xs opacity-60">{role}</p></div>)}</div></div></div></section>; }

export function FinalSection() { return <section id="final" className="px-4 pb-4 sm:px-6 sm:pb-6"><div className="mx-auto max-w-7xl rounded-[28px] bg-primary px-6 py-20 text-center text-primary-foreground shadow-clay sm:px-12"><FileCheck2 className="mx-auto" size={34} /><p className="eyebrow mt-6 text-primary-foreground/70">Built for the moment after</p><h2 className="mx-auto mt-3 max-w-2xl font-display text-4xl font-bold sm:text-5xl">The important part isn&apos;t when the complaint is created. It&apos;s what happens after.</h2><p className="mx-auto mt-5 max-w-xl opacity-75">Give every complaint a record, every delay a timestamp, and every recurring issue a history.</p><div className="mt-8 flex flex-wrap justify-center gap-3"><Button variant="secondary" onClick={() => document.querySelector("#top")?.scrollIntoView({ behavior: "smooth" })}>Explore Tracehold <ArrowRight size={16} /></Button><Button variant="outline" className="border-primary-foreground/40 bg-transparent text-primary-foreground hover:bg-primary-foreground/10 hover:text-primary-foreground" onClick={() => document.querySelector("#architecture")?.scrollIntoView({ behavior: "smooth" })}>View architecture <ArrowRight size={16} /></Button></div></div></section>; }

export function LandingFooter() { return <footer className="mx-auto flex max-w-7xl flex-col gap-8 px-6 py-12 sm:flex-row sm:items-end sm:justify-between"><div><div className="flex items-center gap-3"><span className="grid size-9 place-items-center rounded-xl bg-primary text-primary-foreground"><FileCheck2 size={18} /></span><span className="font-display text-xl font-bold">Tracehold</span></div><p className="mt-4 text-sm text-muted-foreground">Event-driven maintenance escalation and evidence.</p></div><div className="flex flex-wrap gap-x-6 gap-y-2 text-sm text-muted-foreground"><a href="#workflow">Product</a><a href="#story">How it works</a><a href="#architecture">Architecture</a><span>© 2026 Tracehold</span></div></footer>; }
